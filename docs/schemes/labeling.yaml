definitions:
# Schema for Labeling
  Labeling:
    type: "object"
    properties:
      labels:
        type: "array"
        items:
          type: "integer"
        description: "Primary Keys Labels that can be associated with labeling"
    xml:
      name: "Labeling"
