definitions:
# Schema for Video
  Video:
    type: "object"
    properties:
      url:
        type: "string"
        example: "testurl.com"
      offset:
        type: "number"
        example: 0.0
    xml:
      name: "Video"

