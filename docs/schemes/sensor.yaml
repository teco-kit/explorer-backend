definitions:
# Schema for Sensor
  Sensor:
    type: "object"
    properties:
      _id:
        type: "integer"
        format: "int64"
      name:
        type: "string"
    xml:
      name: "Sensor"

