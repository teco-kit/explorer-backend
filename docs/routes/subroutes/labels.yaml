/datasets/{datasetId}/labelings/{labelingId}/labels:
  get:
    tags:
    - "datasets"
    summary: "Returns all labels for a given labeling within a dataset"
    description: "Returns a list of all available labels for a given labeling within a dataset"
    operationId: "getDatasetLabel"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    responses:
      200:
        description: "successful operation"
        schema:
          type: "array"
          items:
            $ref: "#/definitions/DatasetLabel"
    security:
    - api_key: []
  post:
    tags:
    - "datasets"
    summary: "Add a new label"
    description: "Creates a new labels for a given labeling within a dataset"
    operationId: "createDatasetLabel"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    - in: "body"
      name: "body"
      description: "DatasetLabel object that needs to be created"
      required: true
      schema:
        $ref: "#/definitions/DatasetLabel"
    responses:
      201:
        description: "Created"
      405:
        description: "Invalid input"
    security:
    - api_key: []
  put:
    tags:
    - "datasets"
    summary: "Update existing labels for a given labeling within a dataset"
    description: "Updates a bulk of labels for a given labeling within a dataset"
    operationId: "updateDatasetLabel"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    - in: "body"
      name: "body"
      description: "DatasetLabels that need to be updated"
      required: true
      schema:
        type: "array"
        items:
          $ref: "#/definitions/DatasetLabel"
    responses:
      501:
        description: "Not Implemented"
    security:
    - api_key: []
  delete:
    tags:
    - "datasets"
    summary: "Deletes all labels for a given labeling within a dataset"
    description: "Deletes all labels for a given labeling within a dataset"
    operationId: "deleteDatasetLabels"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    responses:
      200:
        description: "deleted all labels for a given labeling within a dataset"
    security:
    - api_key: []

/datasets/{datasetId}/labelings/{labelingId}/labels/{id}:
  get:
    tags:
    - "datasets"
    summary: "Get a label for a given labeling within a dataset"
    description: "Returns a single label for a given labeling within a dataset"
    operationId: "getDatasetLabelById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    - name: "id"
      in: "path"
      description: "ID of label to return"
      required: true
      type: "string"
    responses:
      200:
        description: "successful operation"
        schema:
          $ref: "#/definitions/DatasetLabel"
      400:
        description: "Invalid ID supplied"
      404:
        description: "Dataset, Labeling or Label not found"
    security:
    - api_key: []
  post:
    tags:
    - "datasets"
    summary: "Create a label for a given labeling within a dataset"
    description: "Create a single label for a given labeling within a dataset"
    operationId: "createDatasetLabelById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    - name: "id"
      in: "path"
      description: "ID of label to return"
      required: true
      type: "string"
    responses:
      500:
        description: "Method not allowed"
    security:
    - api_key: []
    deprecated: true
  put:
    tags:
    - "datasets"
    summary: "Update an label for a given labeling within a dataset"
    description: "Update a single label for a given labeling within a dataset"
    operationId: "updateDatasetLabelById"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    - name: "id"
      in: "path"
      description: "ID of label to return"
      required: true
      type: "string"
    responses:
      200:
        description: "successful operation"
        schema:
          $ref: "#/definitions/DatasetLabel"
      400:
        description: "Invalid ID supplied"
      404:
        description: "Dataset, Labeling or Label not found"
    security:
    - api_key: []
  delete:
    tags:
    - "datasets"
    summary: "Deletes a label for a given labeling within a dataset"
    description: "Deletes a specific label for a given labeling within a dataset"
    operationId: "deleteDatasetLabelById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "string"
    - name: "labelingId"
      in: "path"
      description: "ID of associated labeling"
      required: true
      type: "string"
    - name: "id"
      in: "path"
      description: "ID of label to return"
      required: true
      type: "string"
    responses:
      200:
        description: "successful operation"
    security:
    - api_key: []
