/datasets/{datasetId}/labelings:
  get:
    tags:
    - "datasets"
    summary: "Returns all labelings for a given dataset"
    description: "Returns a list of all available labelings for a given dataset"
    operationId: "getDatasetLabelings"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "successful operation"
        schema:
          type: "array"
          items:
            $ref: "#/definitions/DatasetLabeling"
    security:
    - api_key: []
  post:
    tags:
    - "datasets"
    summary: "Add a new labeling"
    description: "Creates a new labeling for a given dataset"
    operationId: "createDatasetLabeling"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "integer"
      format: "int64"
    - in: "body"
      name: "body"
      description: "DatasetLabeling object that needs to be created"
      required: true
      schema:
        $ref: "#/definitions/DatasetLabeling"
    responses:
      201:
        description: "Created"
      405:
        description: "Invalid input"
    security:
    - api_key: []
  put:
    tags:
    - "datasets"
    summary: "Update existing labelings for a given dataset"
    description: "Updates a bulk of labelings for a given dataset"
    operationId: "updateDatasetLabelings"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "integer"
      format: "int64"
    - in: "body"
      name: "body"
      description: "DatasetLabelings that need to be updated"
      required: true
      schema:
        type: "array"
        items:
          $ref: "#/definitions/DatasetLabeling"
    responses:
      501:
        description: "Not Implemented"
    security:
    - api_key: []
  delete:
    tags:
    - "datasets"
    summary: "Deletes all labelings for a given dataset"
    description: "Deletes all labelings for a given dataset"
    operationId: "deleteDatasetLabelings"
    produces:
    - "application/json"
    parameters:
      - name: "datasetId"
        in: "path"
        description: "ID of associated dataset"
        required: true
        type: "integer"
        format: "int64"
    responses:
      200:
        description: "deleted all labeling for given dataset"
    security:
    - api_key: []

/datasets/{datasetId}/labelings/{id}:
  get:
    tags:
    - "datasets"
    summary: "Get an labeling by id for a given dataset"
    description: "Returns a single labeling for a given dataset"
    operationId: "getDatasetLabelingById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of labeling to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "successful operation"
        schema:
          $ref: "#/definitions/DatasetLabeling"
      400:
        description: "Invalid ID supplied"
      404:
        description: "Dataset or Labeling not found"
    security:
    - api_key: []
  post:
    tags:
    - "datasets"
    summary: "Create an labeling by id for a given dataset"
    description: "Create a single labeling by id for a given dataset"
    operationId: "createDatasetLabelingById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of labeling to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      500:
        description: "Method not allowed"
    security:
    - api_key: []
    deprecated: true
  put:
    tags:
    - "datasets"
    summary: "Update an labeling by id for a given dataset"
    description: "Update a single labeling by id for a given dataset"
    operationId: "updateDatasetLabelingById"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of labeling to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "successful operation"
        schema:
          $ref: "#/definitions/DatasetLabeling"
      400:
        description: "Invalid ID supplied"
      404:
        description: "Dataset or Labeling not found"
    security:
    - api_key: []
  delete:
    tags:
    - "datasets"
    summary: "Deletes an labeling by id for a given dataset"
    description: "Deletes a specific evebt by id for a given dataset"
    operationId: "deleteDatasetLabelingById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of associated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of labeling to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "successful operation"
    security:
    - api_key: []
