/datasets/{datasetId}/fusedseries:
  get:
    tags:
    - "datasets"
    summary: "Returns all fusedseries for a given dataset"
    description: "Returns a list of all available fusedseries for a given dataset"
    operationId: "getFusedseries"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of asociated dataset"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "succesful operation"
        schema:
          type: "array"
          items:
            $ref: "#/definitions/Fusedseries"
    security:
    - api_key: []
  post:
    tags:
    - "datasets"
    summary: "Add a new fusedseries"
    description: "Creates a new fusedseries for a given dataset"
    operationId: "createFusedseries"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of asociated dataset"
      required: true
      type: "integer"
      format: "int64"
    - in: "body"
      name: "body"
      description: "Fusedseries object that needs to be created"
      required: true
      schema:
        $ref: "#/definitions/Fusedseries"
    responses:
      201:
        description: "Created"
      405:
        description: "Invalid input"
    security:
    - api_key: []
  put:
    tags:
    - "datasets"
    summary: "Update existing fusedseries for a given dataset"
    description: "Updates a bulk of fusedseries for a given dataset"
    operationId: "updateFusedseries"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of asociated dataset"
      required: true
      type: "integer"
      format: "int64"
    - in: "body"
      name: "body"
      description: "Fusedseries that need to be updated"
      required: true
      schema:
        type: "array"
        items:
          $ref: "#/definitions/Fusedseries"
    responses:
      501:
        description: "Not Implemented"
    security:
    - api_key: []
  delete:
    tags:
    - "datasets"
    summary: "Deletes all fusedseries for a given dataset"
    description: "Deletes all fusedseries for a given dataset"
    operationId: "deleteFusedseries"
    produces:
    - "application/json"
    parameters:
      - name: "datasetId"
        in: "path"
        description: "ID of asociated dataset"
        required: true
        type: "integer"
        format: "int64"
    responses:
      200:
        description: "deleted all fusedseries for given dataset"
    security:
    - api_key: []

/datasets/{datasetId}/fusedseries/{id}:
  get:
    tags:
    - "datasets"
    summary: "Get a fusedseries by id for a given dataset"
    description: "Returns a single fusedseries for a given dataset"
    operationId: "getFusedseriesById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of asociated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of fusedseries to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "succesful operation"
        schema:
          $ref: "#/definitions/Fusedseries"
      400:
        description: "Invalid ID supplied"
      404:
        description: "Dataset or Fusedseries not found"
    security:
    - api_key: []
  post:
    tags:
    - "datasets"
    summary: "Create a fusedseries by id for a given dataset"
    description: "Create a single fusedseries by id for a given dataset"
    operationId: "createFusedseriesById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of asociated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of fusedseries to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      500:
        description: "Method not allowed"
    security:
    - api_key: []
    deprecated: true
  put:
    tags:
    - "datasets"
    summary: "Update a fusedseries by id for a given dataset"
    description: "Update a single fusedseries by id for a given dataset"
    operationId: "updateFusedseriesById"
    consumes:
    - "application/json"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of asociated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of fusedseries to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "succesful operation"
        schema:
          $ref: "#/definitions/Fusedseries"
      400:
        description: "Invalid ID supplied"
      404:
        description: "Dataset or Fusedseries not found"
    security:
    - api_key: []
  delete:
    tags:
    - "datasets"
    summary: "Deletes a fusedseries by id for a given dataset"
    description: "Deletes a specific evebt by id for a given dataset"
    operationId: "deleteFusedseriesById"
    produces:
    - "application/json"
    parameters:
    - name: "datasetId"
      in: "path"
      description: "ID of asociated dataset"
      required: true
      type: "integer"
      format: "int64"
    - name: "id"
      in: "path"
      description: "ID of fusedseries to return"
      required: true
      type: "integer"
      format: "int64"
    responses:
      200:
        description: "succesful operation"
    security:
    - api_key: []
