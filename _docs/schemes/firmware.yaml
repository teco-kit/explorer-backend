definitions:
# Schema for Firmware
  Firmware:
    type: "object"
    properties:
      version:
        type: "string"
        example: "1.0"
      binary:
        type: "string"
      hash:
        type: "string"
      supportedDevices:
        type: "array"
        items:
          type: "integer"
        description: "Generation numbers of device generations supported by firmware"
      uploadedAt:
        type: "string"
    xml:
      name: "Firmware"
